# 🎭 Система ролей та прав доступу

## 📊 Огляд системи

Система має **3 ролі** з різними рівнями доступу:

| Роль | Email | Password | Права |
|------|-------|----------|-------|
| 👨‍💼 **Admin** | `admin@kzvo.edu` | `admin123` | Повні права |
| 👨‍🏫 **Teacher** | `teacher@kzvo.edu` | `teacher123` | Додавання контенту |
| 👨‍🎓 **Student** | `student@kzvo.edu` | `student123` | Базові права |

---

## 👨‍💼 Адміністратор (Admin)

### Повні права доступу:

#### Управління користувачами
- ✅ Перегляд всіх користувачів
- ✅ Призначення/зняття ролей (admin, teacher, student)
- ✅ Видалення користувачів
- ✅ Блокування облікових записів

#### Управління контентом
- ✅ Додавання новин
- ✅ Редагування будь-яких новин
- ✅ Видалення будь-яких новин
- ✅ Додавання документів
- ✅ Редагування документів
- ✅ Видалення документів

#### Управління подіями
- ✅ Створення подій
- ✅ Редагування подій
- ✅ Видалення подій
- ✅ Управління реєстраціями на події

#### Управління конкурсами
- ✅ Створення конкурсів
- ✅ Редагування конкурсів
- ✅ Перегляд заявок
- ✅ Оцінювання робіт

#### Налаштування системи
- ✅ Зміна налаштувань сайту
- ✅ Управління правами доступу
- ✅ Перегляд логів системи
- ✅ Аналітика та статистика

#### Заявки та документи
- ✅ Перегляд всіх заявок
- ✅ Затвердження/відхилення заявок
- ✅ Перегляд завантажених документів

### Доступ до сторінок:
- ✅ `/admin` - Адмін-панель
- ✅ `/admin/users` - Управління користувачами
- ✅ `/admin/content` - Управління контентом
- ✅ `/admin/events` - Управління подіями
- ✅ `/admin/settings` - Налаштування

---

## 👨‍🏫 Вчитель (Teacher)

### Обмежені права для викладачів:

#### Управління контентом
- ✅ Додавання новин
- ✅ Редагування **своїх** новин
- ✅ Додавання документів
- ✅ Редагування **своїх** документів
- ❌ Видалення новин (тільки admin)
- ❌ Видалення документів (тільки admin)

#### Робота з учнями
- ✅ Перегляд заявок студентів
- ✅ Коментування заявок
- ✅ Рекомендації для студентів
- ❌ Затвердження заявок (тільки admin)

#### Управління подіями
- ✅ Перегляд подій
- ✅ Перегляд списку учасників
- ❌ Створення подій (тільки admin)
- ❌ Редагування подій (тільки admin)

#### Обмеження
- ❌ Управління користувачами
- ❌ Призначення ролей
- ❌ Зміна налаштувань системи
- ❌ Видалення будь-якого контенту

### Доступ до сторінок:
- ✅ `/teacher/dashboard` - Панель вчителя
- ✅ `/teacher/news` - Управління новинами
- ✅ `/teacher/documents` - Управління документами
- ✅ `/teacher/students` - Перегляд заявок студентів
- ❌ `/admin` - Заборонено

---

## 👨‍🎓 Студент (Student)

### Базові права для студентів:

#### Особистий кабінет
- ✅ Перегляд свого профілю
- ✅ Редагування профілю
- ✅ Зміна пароля
- ✅ Завантаження фото профілю

#### Заявки та реєстрації
- ✅ Подача заявок на події
- ✅ Реєстрація на конкурси
- ✅ Завантаження документів (заявки, портфоліо)
- ✅ Перегляд статусу своїх заявок
- ✅ Редагування незатверджених заявок
- ❌ Видалення затверджених заявок

#### Перегляд контенту
- ✅ Перегляд новин
- ✅ Перегляд подій
- ✅ Перегляд документів
- ✅ Завантаження публічних документів
- ❌ Додавання контенту

#### Обмеження
- ❌ Додавання новин
- ❌ Додавання документів (крім особистих)
- ❌ Управління подіями
- ❌ Адміністративні функції
- ❌ Перегляд даних інших користувачів

### Доступ до сторінок:
- ✅ `/profile` - Особистий профіль
- ✅ `/student/applications` - Мої заявки
- ✅ `/student/documents` - Мої документи
- ✅ `/student/events` - Доступні події
- ❌ `/admin` - Заборонено
- ❌ `/teacher` - Заборонено

---

## 🔐 Firestore Security Rules

### Правила безпеки для колекцій:

#### Users Collection
```javascript
// Користувачі можуть читати тільки свої дані
// Адміни можуть читати всі дані
allow read: if isOwner(userId) || isAdmin();

// Користувачі можуть оновлювати свій профіль (крім ролі)
// Адміни можуть оновлювати будь-який профіль
allow update: if (isOwner(userId) && !changingRole()) || isAdmin();
```

#### News Collection
```javascript
// Всі можуть читати новини
allow read: if true;

// Адміни та вчителі можуть додавати новини
allow create: if canManageContent();

// Автори можуть редагувати свої новини, адміни - будь-які
allow update: if (isAuthor() || isAdmin());

// Тільки адміни можуть видаляти
allow delete: if isAdmin();
```

#### Documents Collection
```javascript
// Всі можуть читати документи
allow read: if true;

// Адміни та вчителі можуть додавати/редагувати/видаляти
allow create, update, delete: if canManageContent();
```

#### Submissions Collection
```javascript
// Користувачі бачать свої заявки
// Вчителі та адміни бачать всі заявки
allow read: if isOwner() || canManageContent();

// Студенти можуть створювати заявки
allow create: if isAuthenticated();

// Користувачі можуть редагувати незатверджені заявки
// Адміни можуть редагувати будь-які
allow update: if (isOwner() && !isLocked()) || isAdmin();

// Тільки адміни можуть видаляти
allow delete: if isAdmin();
```

---

## 🚀 Створення тестових користувачів

### Варіант 1: Через Firebase Console

1. **Authentication → Users → Add user**
   - Створіть 3 користувачі з email/password

2. **Firestore Database → users**
   - Для кожного користувача створіть документ з UID
   - Встановіть відповідну роль: `admin`, `teacher`, `student`

### Варіант 2: Через сайт

1. Зареєструйтеся на сайті з потрібним email
2. В Firestore вручну змініть поле `role`

**Детальна інструкція:** `scripts/create-test-users.md`

---

## 🧪 Тестування ролей

### Тест 1: Адміністратор
```bash
1. Увійдіть як admin@kzvo.edu / admin123
2. Перевірте доступ до /admin
3. Спробуйте змінити роль користувача
4. Додайте новину
5. Видаліть документ
```

### Тест 2: Вчитель
```bash
1. Увійдіть як teacher@kzvo.edu / teacher123
2. Спробуйте відкрити /admin (має бути заборонено)
3. Додайте новину
4. Спробуйте редагувати свою новину (має працювати)
5. Спробуйте видалити новину (має бути заборонено)
```

### Тест 3: Студент
```bash
1. Увійдіть як student@kzvo.edu / student123
2. Спробуйте відкрити /admin (має бути заборонено)
3. Подайте заявку на подію
4. Завантажте документ
5. Спробуйте додати новину (має бути заборонено)
```

---

## 📝 Використання в коді

### Перевірка ролі в компонентах

```tsx
import { useAuth } from '../context/AuthContext';

function MyComponent() {
  const { isAdmin, isTeacher, isStudent } = useAuth();
  
  return (
    <div>
      {isAdmin && <AdminButton />}
      {isTeacher && <TeacherButton />}
      {isStudent && <StudentButton />}
    </div>
  );
}
```

### Захист роутів

```tsx
// Тільки для адмінів
<Route path="/admin" element={
  <ProtectedRoute requireAdmin={true}>
    <AdminPanel />
  </ProtectedRoute>
} />

// Для вчителів та адмінів
<Route path="/teacher" element={
  <ProtectedRoute requireRole="teacher">
    <TeacherDashboard />
  </ProtectedRoute>
} />
```

---

## 🔄 Зміна ролі користувача

### Через адмін-панель
1. Увійдіть як адміністратор
2. Перейдіть в `/admin`
3. Знайдіть користувача
4. Змініть роль

### Через Firestore Console
1. Відкрийте Firestore Database
2. Колекція `users` → Документ користувача
3. Змініть поле `role` на: `admin`, `teacher`, або `student`

---

## 🎯 Підсумок

✅ **Створено 3 ролі** з різними правами  
✅ **Налаштовано Firestore Security Rules**  
✅ **Додано перевірки в коді**  
✅ **Створено документацію**  
✅ **Підготовлено тестові облікові записи**  

**Система готова до використання!** 🚀

