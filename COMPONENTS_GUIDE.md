# 📚 Посібник по компонентах

## Оновлена структура проєкту

### ✅ Компоненти

#### 1. **Header** (`src/components/Header.tsx`)

Оновлений компонент шапки сайту з розширеним функціоналом:

**Основні можливості:**
- **Логотип** з іконкою та назвою академії
- **8 пунктів меню**:
  1. ПРО АКАДЕМІЮ → `/about`
  2. ДІЯЛЬНІСТЬ → `/activity`
  3. ОСВІТА ПРОГРАМИ → `/education`
  4. ВЧИТЕЛЮ → `/teachers`
  5. УЧНЯМ/КОНКУРСИ → `/students`
  6. РЕСУРСИ → `/resources`
  7. НОВИНИ → `/news`
  8. КОНТАКТИ → `/contacts`

- **Пошук**: Кнопка з іконкою Search, розгортається у рядок пошуку
- **Перемикач мови**: UA/EN у верхній панелі
- **Мобільне меню**: Бургер-меню для екранів < 1024px
- **Адаптивність**: Повністю responsive дизайн

**Особливості:**
```tsx
// Стан перемикача мови
const [currentLang, setCurrentLang] = useState<'UA' | 'EN'>('UA');

// Стан пошуку
const [isSearchOpen, setIsSearchOpen] = useState(false);

// Стан мобільного меню
const [isMenuOpen, setIsMenuOpen] = useState(false);
```

---

#### 2. **Breadcrumbs** (`src/components/Breadcrumbs.tsx`)

Хлібні крошки для навігації по сайту:

**Функціонал:**
- Автоматично генерує шлях на основі поточного URL
- Показує іконку "Домівка" для головної сторінки
- Не відображається на головній сторінці
- Підсвічує поточну сторінку

**Маппінг шляхів:**
```tsx
const pathLabels: Record<string, string> = {
  'about': 'Про академію',
  'activity': 'Діяльність',
  'education': 'Освіта програми',
  'teachers': 'Вчителю',
  'students': 'Учням/Конкурси',
  'resources': 'Ресурси',
  'news': 'Новини',
  'contacts': 'Контакти',
};
```

**Приклад відображення:**
```
🏠 > Про академію > Історія
```

---

#### 3. **Footer** (`src/components/Footer.tsx`)

Оновлений підвал з розширеним функціоналом:

**4 колонки:**

1. **Контакти**:
   - Адреса: м. Київ, вул. Освітня, буд. 1
   - Телефон: +380 (44) 123-45-67
   - Email: info@academy.ua

2. **Швидкі посилання**:
   - Про академію
   - Діяльність
   - Освіта програми
   - Новини

3. **Ресурси**:
   - Вчителю
   - Учням
   - Ресурси
   - Контакти

4. **Соціальні мережі**:
   - Facebook
   - Instagram
   - YouTube
   - LinkedIn

**Додаткові посилання:**
- Політика конфіденційності
- Умови використання
- Копірайт: © 2024 Академія Педагогічної Освіти

---

#### 4. **MainLayout** (`src/layouts/MainLayout.tsx`)

Основний макет, що обгортає всі сторінки:

```tsx
<div className="flex flex-col min-h-screen">
  <Header />
  <Breadcrumbs />
  <main className="flex-grow bg-white">
    <Outlet /> {/* Тут рендериться контент сторінок */}
  </main>
  <Footer />
</div>
```

**Flexbox структура:**
- `min-h-screen` - мінімальна висота екрану
- `flex-grow` - контент займає всю доступну висоту
- Footer завжди внизу сторінки

---

### 📄 Сторінки

#### 1. **Home** (`src/pages/Home.tsx`) - Головна
**Секції:**
- Hero-секція з великим заголовком та CTA
- 3 швидкі посилання (для вчителів, учнів, програми)
- 4 переваги академії
- Останні новини (3 картки)
- CTA секція з призивом до дії

#### 2. **AboutPage** (`src/pages/AboutPage.tsx`) - Про академію
**Контент:**
- Опис академії
- Місія та цілі
- Інформація про випускників

#### 3. **ActivityPage** (`src/pages/ActivityPage.tsx`) - Діяльність
**4 блоки діяльності:**
- Освітня діяльність
- Методична робота
- Наукова діяльність
- Міжнародна співпраця

#### 4. **EducationPage** (`src/pages/EducationPage.tsx`) - Освіта програми
**Програми:**
- Бакалаврат (4 роки)
- Магістратура (2 роки)
- Курси підвищення кваліфікації (1-6 місяців)

#### 5. **TeachersPage** (`src/pages/TeachersPage.tsx`) - Вчителю
**Розділи:**
- Методичні матеріали
- Професійний розвиток
- Сертифікація
- Інноваційні підходи
- Форма підписки на оновлення

#### 6. **StudentsPage** (`src/pages/StudentsPage.tsx`) - Учням/Конкурси
**Типи подій:**
- Олімпіади
- Творчі конкурси
- Наукові проекти
- Майстер-класи
- Кнопка реєстрації
- Додаткова інформація (правила, переможці, календар)

#### 7. **ResourcesPage** (`src/pages/ResourcesPage.tsx`) - Ресурси
**Категорії:**
- Методичні посібники (150+ документів)
- Відеоматеріали (80+ відео)
- Завантаження (200+ файлів)
- Електронна бібліотека (500+ видань)
- Форма пошуку ресурсів

#### 8. **NewsPage** (`src/pages/NewsPage.tsx`) - Новини
**Функціонал:**
- Сітка новин (2 колонки)
- Фільтр за категоріями
- Дата публікації
- Категорія новини
- Пагінація

#### 9. **ContactsPage** (`src/pages/ContactsPage.tsx`) - Контакти
**2 колонки:**

**Ліва:**
- Контактна інформація
- Адреса з деталями
- Телефони
- Email
- Режим роботи
- Місце для карти

**Права:**
- Контактна форма:
  - Ім'я *
  - Email *
  - Телефон
  - Тема звернення *
  - Повідомлення *
  - Кнопка "Надіслати"

---

### 🎨 Стилізація

#### Додані анімації в `index.css`:

```css
.animate-fadeIn {
  animation: fadeIn 0.3s ease-in-out;
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(-10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

**Використання:**
- Пошукове поле (Header)
- Мобільне меню (Header)
- Плавна поява елементів

#### Smooth scroll:
```css
html {
  scroll-behavior: smooth;
}
```

---

### 🛣 Маршрутизація

**Структура роутів в `App.tsx`:**

```tsx
<Router>
  <Routes>
    <Route path="/" element={<MainLayout />}>
      <Route index element={<Home />} />
      <Route path="about" element={<AboutPage />} />
      <Route path="activity" element={<ActivityPage />} />
      <Route path="education" element={<EducationPage />} />
      <Route path="teachers" element={<TeachersPage />} />
      <Route path="students" element={<StudentsPage />} />
      <Route path="resources" element={<ResourcesPage />} />
      <Route path="news" element={<NewsPage />} />
      <Route path="contacts" element={<ContactsPage />} />
    </Route>
  </Routes>
</Router>
```

---

### 🎯 Іконки Lucide React

**Використовувані іконки:**
- `GraduationCap` - Логотип академії
- `Menu`, `X` - Мобільне меню
- `Search` - Пошук
- `Home` - Головна (breadcrumbs)
- `ChevronRight` - Розділювач (breadcrumbs)
- `Mail`, `Phone`, `MapPin`, `Clock` - Контакти
- `Facebook`, `Instagram`, `Youtube`, `Linkedin` - Соцмережі
- `BookOpen`, `Users`, `Award`, `TrendingUp` - Переваги
- `Trophy`, `Star`, `Calendar`, `Lightbulb` - Розділи
- `FileText`, `Video`, `Download`, `Book` - Ресурси
- `ArrowRight` - Посилання
- `Tag` - Категорії новин

---

### ✅ Що готово

- ✅ Header з 8 пунктами меню
- ✅ Перемикач мови UA/EN
- ✅ Функція пошуку
- ✅ Мобільне меню (burger)
- ✅ Breadcrumbs (хлібні крошки)
- ✅ Footer з соцмережами
- ✅ 8 повноцінних сторінок
- ✅ Роутинг налаштовано
- ✅ MainLayout з усіма компонентами
- ✅ Адаптивний дизайн
- ✅ Анімації та hover-ефекти
- ✅ Проєкт успішно збирається

---

### 🚀 Запуск

```bash
npm run dev
```

Сайт буде доступний за адресою: http://localhost:5173

---

## 📱 Адаптивність

**Breakpoints:**
- Mobile: < 768px
- Tablet: 768px - 1024px
- Desktop: > 1024px

**Адаптовані елементи:**
- Header (мобільне меню)
- Всі сторінки (grid → 1 колонка)
- Footer (4 колонки → 2 → 1)
- Форми (повна ширина на мобільних)

---

## 🎨 Кольори

- **Primary**: `#1e3a8a` (синій)
- **Accent**: `#fbbf24` (жовтий)
- **Gray**: відтінки сірого для тексту
- **White**: `#ffffff` для фону

Всі кольори налаштовані в `tailwind.config.js` та доступні через Tailwind класи.

