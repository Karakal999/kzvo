# 🔐 Налаштування Головного Адміністратора

## Важливо: admin@kzvo.edu

Користувач `admin@kzvo.edu` є **головним суперадміністратором** з повними правами:

### Права доступу:

✅ **Повний доступ до всіх розділів:**
- Управління всіма користувачами
- Зміна ролей користувачів
- Видалення користувачів
- Управління новинами, подіями, документами
- Доступ до системних налаштувань
- Управління вчителями та програмами
- Модерація всіх заявок

✅ **Спеціальні можливості:**
- Може змінювати ролі інших адміністраторів
- Може створювати нових адміністраторів
- Має доступ до критичних налаштувань системи
- Не може бути видалений іншими адмінами

---

## Як створити головного адміністратора:

### Крок 1: Створити користувача в Firebase

1. Відкрийте **Firebase Console** → **Authentication**
2. Натисніть "Add user"
3. Email: `admin@kzvo.edu`
4. Password: `admin123` (або ваш безпечний пароль)
5. Натисніть "Add user"

### Крок 2: Встановити роль в Firestore

1. Відкрийте **Firebase Console** → **Firestore Database**
2. Перейдіть до колекції `users`
3. Знайдіть документ з email `admin@kzvo.edu`
4. Додайте/оновіть поля:
   ```
   {
     email: "admin@kzvo.edu",
     displayName: "Головний Адміністратор",
     role: "admin",
     createdAt: (timestamp),
     lastLogin: (timestamp)
   }
   ```

### Крок 3: Розгорнути правила безпеки

1. У Firebase Console перейдіть до **Firestore Database** → **Rules**
2. Скопіюйте вміст з файлу `firestore.rules`
3. Натисніть "Publish"

---

## Перевірка доступу:

Після налаштування, admin@kzvo.edu матиме:

### В Адмін-панелі (`/admin`):
- ✅ Управління користувачами (перегляд, редагування, видалення)
- ✅ Зміна ролей (admin, teacher, student)
- ✅ Додавання нових адміністраторів
- ✅ Управління контентом (новини, події, документи)
- ✅ Системні налаштування

### В Профілі (`/profile`):
- ✅ Статистика по всій системі
- ✅ Повна історія активності
- ✅ Доступ до всіх налаштувань

---

## Безпека:

⚠️ **ВАЖЛИВО:**

1. **Змініть пароль** після першого входу
2. **Не діліться** доступом до admin@kzvo.edu
3. **Використовуйте** сильний пароль (мінімум 12 символів)
4. **Увімкніть** двофакторну автентифікацію в Firebase Console
5. **Регулярно переглядайте** журнал активності

---

## Додавання інших адміністраторів:

Тільки admin@kzvo.edu може створювати нових адміністраторів:

1. Увійдіть як admin@kzvo.edu
2. Перейдіть в `/admin` → "Користувачі"
3. Знайдіть користувача
4. Натисніть "Змінити роль" → "admin"
5. Підтвердіть зміни

---

## Структура ролей:

```
admin@kzvo.edu (Super Admin)
    ↓
  admin (Regular Admin)
    ↓
  teacher (Вчитель)
    ↓
  student (Студент)
```

---

## Технічні деталі:

### В коді (AuthContext):
```typescript
const isSuperAdmin = userProfile?.email === 'admin@kzvo.edu' && isAdmin;
```

### В Firestore Rules:
```javascript
function isSuperAdmin() {
  return isAuthenticated() && 
         request.auth.token.email == 'admin@kzvo.edu' &&
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
}
```

---

## Довідка:

- Email: admin@kzvo.edu
- Role: admin (super admin)
- Права: Повні (read/write/delete всюди)
- Можна видалити: Ні
- Може змінювати ролі: Так

---

**Створено:** 28 грудня 2025
**Статус:** Активний

