# 🛠️ Посібник з керування перекладами

## Огляд

Повна система інструментів для керування перекладами в development та production режимах.

---

## 🎯 Возможности

- ✅ Скрипты синхронизации переводов
- ✅ Автоматическое добавление новых ключей
- ✅ Поиск недостающих переводов
- ✅ Экспорт в CSV для переводчиков
- ✅ Режим отладки переводов (dev only)
- ✅ Визуальная подсветка непереведенных ключей
- ✅ Debug панель с инструментами
- ✅ Отслеживание отсутствующих ключей

---

## 📜 NPM Scripts

### Синхронизация переводов

```bash
# Синхронизировать все переводы
npm run i18n:sync

# Проверить недостающие переводы
npm run i18n:check-missing

# Экспортировать переводы в CSV
npm run i18n:export-csv
```

### Другие скрипты

```bash
# Извлечь ключи из кода
npm run i18n:extract

# Сгенерировать недостающие ключи
npm run i18n:generate

# Проверить використання ключей
npm run i18n:check
```

---

## 🔄 Скрипт синхронизации

**Файл:** `scripts/sync-translations.cjs`

### Що делает:

1. **Добавляет відсутні ключи**
   - Сканирует файлы дефолтного языка (uk)
   - Добавляет недостающие ключи в другие языки
   - Помечает новые ключи префиксом `[EN]`

2. **Находит устаревшие ключи**
   - Определяет ключи, которые есть в переводах, но отсутствуют в исходном языке
   - Выводит список для ручной проверки

3. **Генерирует отчеты**
   - Подробный отчет по каждому namespace
   - Статистика добавленных/отсутствующих ключей

### Приклады использования:

```bash
# Полная синхронизация
npm run i18n:sync

# Output:
# 🔄 Syncing translations...
# 
# 📦 Processing namespace: common
#   ✓ en: Added 3 missing keys
#   ⚠  en: 1 outdated keys found
# 
# 📊 Translation Report
# ==================================================
# Added keys: 3
# Missing translations (3):
#   common (en): 3 keys
```

### Проверка недостающих:

```bash
npm run i18n:check-missing

# Output:
# 🔍 Checking for missing translations...
# 
# Found missing translations:
# 
# common (en):
#   - buttons.new_feature
#   - labels.new_field
# 
# pages (en):
#   - home.new_section
```

### Экспорт в CSV:

```bash
npm run i18n:export-csv

# Output:
# 📤 Exporting translations to CSV...
# ✓ Exported to: /path/to/translations-export/translations.csv
```

**Формат CSV:**

```csv
Namespace,Key,uk,en
common,buttons.read_more,"Читати далі","Read more"
common,buttons.download,"Завантажити","Download"
```

---

## 🐛 Режим отладки переводов

### TranslationDebugContext

**Файл:** `src/contexts/TranslationDebugContext.tsx`

Предоставляет:
- Режим отладки переводов
- Отслеживание отсутствующих ключей
- Настройки визуализации

### Використання:

```tsx
import { TranslationDebugProvider } from '@/contexts/TranslationDebugContext';

// В main.tsx или App.tsx
<TranslationDebugProvider enableInDev>
  <App />
</TranslationDebugProvider>
```

### useTranslationDebug Hook

```tsx
import { useTranslationDebug } from '@/contexts/TranslationDebugContext';

const MyComponent = () => {
  const {
    isTranslationMode,        // Включен ли режим отладки
    toggleTranslationMode,    // Переключить режим
    missingKeys,             // Список отсутствующих ключей
    addMissingKey,           // Добавить отсутствующий ключ
    clearMissingKeys,        // Очистить список
    showKeysOnHover,         // Показывать ключи при наведении
    toggleShowKeysOnHover,   // Переключить показ ключей
    highlightUntranslated,   // Подсвечивать непереведенные
    toggleHighlightUntranslated, // Переключить подсветку
  } = useTranslationDebug();

  return (
    <div>
      <button onClick={toggleTranslationMode}>
        Toggle Translation Mode
      </button>
      
      {missingKeys.length > 0 && (
        <div>
          Missing keys: {missingKeys.length}
        </div>
      )}
    </div>
  );
};
```

---

## 🎨 Debug Panel

**Файл:** `src/components/TranslationDebugPanel.tsx`

Плавающая панель инструментов (только в dev режиме).

### Добавить в приложение:

```tsx
import TranslationDebugPanel from '@/components/TranslationDebugPanel';

function App() {
  return (
    <>
      <YourApp />
      
      {/* Debug panel - только в dev */}
      {import.meta.env.DEV && <TranslationDebugPanel />}
    </>
  );
}
```

### Возможности панели:

1. **Translation Mode Toggle**
   - Включает/выключает режим отладки
   - Сохраняется в localStorage

2. **Show Keys on Hover**
   - Показывает ключи перевода при наведении

3. **Highlight Untranslated**
   - Подсвечивает непереведенные ключи красным

4. **Missing Keys List**
   - Список всех отсутствующих ключей
   - Показывает namespace:key
   - Время обнаружения

5. **Quick Actions**
   - Copy - копировать список в буфер
   - Export - экспортировать в JSON
   - Clear - очистить список

6. **Links**
   - Открыть редактор переводов
   - Открыть файлы на GitHub

---

## 📊 Визуальная индикация

### Подсветка непереведенных ключей

Когда `highlightUntranslated` включен:

```css
/* Автоматично добавляется в <head> */
[data-translation-key] {
  outline: 1px dashed rgba(255, 0, 0, 0.3);
}

[data-translation-key]:hover {
  outline: 2px solid rgba(255, 0, 0, 0.6);
  background-color: rgba(255, 255, 0, 0.1);
}

[data-translation-missing="true"] {
  background-color: rgba(255, 0, 0, 0.2);
  outline: 2px solid red;
}
```

### Використання в компонентах:

```tsx
const MyComponent = () => {
  const { t } = useTranslation();
  const { highlightUntranslated } = useTranslationDebug();

  return (
    <div 
      data-translation-key={highlightUntranslated ? 'common:buttons.submit' : undefined}
      data-translation-missing={!t('common:buttons.submit')}
    >
      {t('common:buttons.submit')}
    </div>
  );
};
```

---

## 🔧 Workflow для переводчиков

### 1. Разработчик добавляет новый код

```tsx
// Добавил новую кнопку
<button>{t('buttons.new_feature')}</button>
```

### 2. Запустить синхронизацию

```bash
npm run i18n:sync
```

Автоматично добавляет ключ во все языки:

**uk/common.json:**
```json
{
  "buttons": {
    "new_feature": "Нова функція"
  }
}
```

**en/common.json:**
```json
{
  "buttons": {
    "new_feature": "[EN] Нова функція"
  }
}
```

### 3. Экспортировать для переводчиков

```bash
npm run i18n:export-csv
```

Создается файл `translations-export/translations.csv`:

```csv
Namespace,Key,uk,en
common,buttons.new_feature,"Нова функція","[EN] Нова функція"
```

### 4. Переводчики заполняют CSV

```csv
Namespace,Key,uk,en
common,buttons.new_feature,"Нова функція","New Feature"
```

### 5. Імпорт обратно

Вручную обновить файлы или написать скрипт імпорта.

### 6. Проверка

```bash
npm run i18n:check-missing
```

Должно показать "All translations are complete!"

---

## 🎯 Best Practices

### ✅ DO

1. **Регулярно синхронизируйте**
   ```bash
   npm run i18n:sync
   ```

2. **Проверяйте перед коммитом**
   ```bash
   npm run i18n:check-missing
   ```

3. **Використовуйте debug panel в разработке**
   ```tsx
   {import.meta.env.DEV && <TranslationDebugPanel />}
   ```

4. **Экспортируйте для переводчиков**
   ```bash
   npm run i18n:export-csv
   ```

5. **Проверяйте outdated keys**
   - Скрипт покажет ключи, которые больше не используются
   - Удалите их вручную

### ❌ DON'T

1. **Не коммитьте неполные переводы**
   ```bash
   # Всегда проверяйте перед коммитом
   npm run i18n:check-missing
   ```

2. **Не редактируйте файлы напрямую в production**
   - Використовуйте dev режим и скрипты

3. **Не забывайте про префикс [EN]**
   - Это маркер непереведенных ключей
   - Найдите и переведите все такие ключи

---

## 💻 Интеграция с сервисами

### Translation.io

```bash
# Установить
npm install translation-io

# Конфигурация в package.json
{
  "translation": {
    "api_key": "YOUR_API_KEY",
    "source_locale": "uk",
    "target_locales": ["en"]
  }
}

# Синхронизация
npm run translation:sync
```

### Локалізація файлов

```bash
# Push переводов на сервис
npm run translation:push

# Pull переводов с сервиса
npm run translation:pull
```

### Crowdin

```yaml
# crowdin.yml
project_id: "your-project-id"

files:
  - source: /src/locales/uk/**/*.json
    translation: /src/locales/%two_letters_code%/**/%original_file_name%
```

### Lokalise

```javascript
// lokalise.config.js
module.exports = {
  apiKey: process.env.LOKALISE_API_KEY,
  projectId: 'YOUR_PROJECT_ID',
  
  files: [{
    file: 'src/locales/<lang>/<namespace>.json',
    fileFormat: 'json'
  }]
};
```

---

## 📊 Структура файлов

```
src/
├── locales/
│   ├── uk/
│   │   ├── common.json
│   │   ├── navigation.json
│   │   └── ...
│   └── en/
│       ├── common.json
│       └── ...
├── contexts/
│   └── TranslationDebugContext.tsx
└── components/
    └── TranslationDebugPanel.tsx

scripts/
├── sync-translations.cjs       # Синхронизация
├── extract-i18n-keys.cjs      # Извлечение ключей
└── generate-missing-keys.cjs   # Генерация

translations-export/
└── translations.csv            # Экспорт для переводчиков
```

---

## 🎉 Примеры использования

### Приклад 1: Ежедневная разработка

```bash
# Утро - начало работы
npm run i18n:check-missing

# Работа...
# Добавили новые t('key') в код

# Перед обедом - синхронизация
npm run i18n:sync

# Вечер - проверка перед коммитом
npm run i18n:check-missing
git add .
git commit -m "feat: добавлена новая функция"
```

### Приклад 2: Подготовка для переводчиков

```bash
# 1. Синхронизировать все ключи
npm run i18n:sync

# 2. Проверить що добавилось
npm run i18n:check-missing

# 3. Экспортировать в CSV
npm run i18n:export-csv

# 4. Отправить translations-export/translations.csv переводчикам
```

### Приклад 3: Після получения переводов

```bash
# 1. Обновить JSON файлы из CSV
# (вручную или через скрипт)

# 2. Проверить що все переведено
npm run i18n:check-missing

# 3. Проверить build
npm run build
```

---

## 📈 Статистика

**Створено:**
- ✅ 1 скрипт синхронизации (300+ рядків)
- ✅ 1 контекст отладки (200+ рядків)
- ✅ 1 debug панель (250+ рядків)
- ✅ 3 NPM скрипта
- ✅ Документация

**Возможности:**
- ✅ Авто-синхронизация переводов
- ✅ Поиск недостающих ключей
- ✅ Экспорт в CSV
- ✅ Визуальная подсветка
- ✅ Отслеживание missing keys
- ✅ Dev-only режим

---

## ✅ Build Status

```bash
npm run build
# ✅ Build успешен (1.71s)
```

---

## 🚀 Готово!

Повна система керування перекладами готова до використання!

**Наступні кроки:**
1. Додайте `<TranslationDebugProvider>` в main.tsx
2. Додайте `<TranslationDebugPanel />` в App.tsx
3. Запустіть `npm run i18n:sync`
4. Увімкніть Translation Mode в dev режимі
5. Перевірте відсутні ключі

**Успішної роботи з перекладами!** 🚀

---

_Створено: 19 грудня 2025_
_Версия: 1.0.0_
_Статус: Production Ready ✅_

