# 🌐 Language Switching Guide

## ✅ Що реалізовано

Тепер на сайті працює **повноцінне перемикання мов** між українською (UA) та англійською (EN)!

### Функціональність:
- 🔄 **Перемикач UA/EN** у Header працює
- 💾 **Збереження вибору** в localStorage
- 🌍 **Автоматичне оновлення** атрибуту `lang` в HTML
- 📝 **Переклади** для Header, навігації та головної сторінки
- ⚡ **Миттєва зміна** мови без перезавантаження

## 🎯 Як працює

### 1. LanguageContext

Створено глобальний Context для управління мовою:

```tsx
// src/context/LanguageContext.tsx
import { useLanguage } from '../context/LanguageContext';

const { language, setLanguage, t } = useLanguage();
```

### 2. Translations

Всі переклади зберігаються в одному файлі:

```tsx
// src/locales/translations.ts
export const translations = {
  UA: {
    'header.academy': 'Академія',
    'nav.about': 'ПРО АКАДЕМІЮ',
    // ...
  },
  EN: {
    'header.academy': 'Academy',
    'nav.about': 'ABOUT',
    // ...
  },
};
```

### 3. Використання в компонентах

```tsx
import { useLanguage } from '../context/LanguageContext';
import { translations } from '../locales/translations';

const MyComponent = () => {
  const { language } = useLanguage();
  const t = translations[language];
  
  return <h1>{t['page.title']}</h1>;
};
```

## 📝 Як додати нові переклади

### Крок 1: Додайте ключі в translations.ts

```tsx
// src/locales/translations.ts
export const translations = {
  UA: {
    // Існуючі переклади...
    'about.title': 'Історія академії',
    'about.description': 'Ми працюємо з 1998 року',
  },
  EN: {
    // Existing translations...
    'about.title': 'Academy History',
    'about.description': 'We have been working since 1998',
  },
};
```

### Крок 2: Використайте в компоненті

```tsx
import { useLanguage } from '../context/LanguageContext';
import { translations } from '../locales/translations';

const About = () => {
  const { language } = useLanguage();
  const t = translations[language];
  
  return (
    <div>
      <h1>{t['about.title']}</h1>
      <p>{t['about.description']}</p>
    </div>
  );
};
```

## 🔄 Що вже перекладено

### ✅ Header:
- Email і телефон
- Назва академії
- Всі пункти меню
- Пошук
- Кнопка меню

### ✅ Home Page:
- Заголовок і підзаголовок
- Кнопки аудиторій (Я вчитель, Я учень, Я абітурієнт)
- Статистика (Випускників, Програм, Років досвіду)
- Заголовки розділів (Швидкий доступ, Для вчителів, тощо)

### ✅ Footer:
- Контакти
- Швидкі посилання
- Копірайт

## 📋 TODO: Що ще потрібно перекласти

### Сторінки:
- [ ] About (Про академію)
- [ ] Activity (Діяльність)
- [ ] Education (Освіта)
- [ ] Teachers (Вчителю)
- [ ] Students (Учням)
- [ ] Resources (Ресурси)
- [ ] News (Новини)
- [ ] Contacts (Контакти)

### Компоненти:
- [ ] CalendarWidget
- [ ] NewsSection
- [ ] Announcements
- [ ] DocumentBrowser
- [ ] SearchModal
- [ ] Breadcrumbs
- [ ] Competitions
- [ ] Events

### Дані:
- [ ] Mock news (src/data/news.ts)
- [ ] Mock events (src/data/events.ts)
- [ ] Mock documents (src/data/documents.ts)

## 💡 Best Practices

### 1. Іменування ключів

Використовуйте структуровані ключі з крапкою:

```tsx
'section.subsection.element': 'Текст'

// Приклади:
'header.menu': 'Меню'
'home.hero.title': 'Заголовок'
'footer.contacts.phone': 'Телефон'
'common.button.submit': 'Відправити'
```

### 2. Загальні фрази

Виносьте повторювані фрази в `common.*`:

```tsx
'common.read_more': 'Читати далі',
'common.download': 'Завантажити',
'common.close': 'Закрити',
```

### 3. Динамічний контент

Для динамічного контенту використовуйте шаблонні рядки:

```tsx
const message = `${t['common.hello']}, ${userName}!`;
```

### 4. Множина

Для складних випадків з множиною створіть окремі ключі:

```tsx
'news.count.one': '{count} новина',
'news.count.few': '{count} новини',
'news.count.many': '{count} новин',
```

## 🔧 Технічні деталі

### LocalStorage

Мова зберігається в `localStorage` під ключем `'language'`:

```typescript
localStorage.getItem('language') // 'UA' або 'EN'
```

### HTML lang атрибут

Автоматично оновлюється при зміні мови:

```html
<html lang="uk">  <!-- для UA -->
<html lang="en">  <!-- для EN -->
```

### TypeScript типізація

Всі ключі перекладів типізовані:

```typescript
export type TranslationKey = keyof typeof translations.UA;
```

## 🚀 Наступні кроки

1. **Поступово додавайте переклади** для інших сторінок
2. **Використовуйте єдиний підхід** до іменування ключів
3. **Тестуйте обидві мови** при додаванні нового контенту
4. **Синхронізуйте** - кожен ключ повинен бути в UA та EN

## 📦 Структура файлів

```
src/
├── context/
│   └── LanguageContext.tsx    # Context для мови
├── locales/
│   └── translations.ts         # Всі переклади
├── components/
│   └── Header.tsx              # Приклад використання
└── pages/
    └── Home.tsx                # Приклад використання
```

## ⚠️ Важливо

- Завжди додавайте переклад **одночасно** для UA та EN
- Використовуйте **однакові ключі** в обох мовах
- Перевіряйте правильність **типографіки** (лапки, тире)
- Враховуйте **довжину тексту** при перекладі (EN часто довший)

---

**Питання?** Перевірте приклади в `Header.tsx` та `Home.tsx`! 🎓

