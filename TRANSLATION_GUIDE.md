# 🌐 Посібник з перекладу контенту

## ✅ Що вже зроблено:

### 1. **Утиліти для багатомовного контенту**
- ✅ `src/utils/multilingualData.ts` - хук `useMultilingualContent()` і функція `ml()`
- ✅ Оновлені типи в `src/types/index.ts` для підтримки `MultilingualContent`

### 2. **Перекладені компоненти:**
- ✅ `Home.tsx` - головна сторінка
- ✅ `NewsSection.tsx` - секція новин
- ✅ `CalendarWidget.tsx` - віджет календаря

### 3. **Перекладені дані:**
- ✅ Події (перші 6 подій у `src/data/events.ts`)

---

## 📝 Як перекладати динамічні дані:

### Крок 1: Імпортуйте функцію `ml`

```typescript
import { ml } from '../utils/multilingualData';
```

### Крок 2: Обгорніть рядки в `ml()`

**До:**
```typescript
{
  id: '1',
  title: 'Вебінар: Сучасні методи викладання',
  description: 'Онлайн-вебінар для вчителів',
  location: 'Онлайн (Zoom)',
}
```

**Після:**
```typescript
{
  id: '1',
  title: ml('Вебінар: Сучасні методи викладання', 'Webinar: Modern Teaching Methods'),
  description: ml('Онлайн-вебінар для вчителів', 'Online webinar for teachers'),
  location: ml('Онлайн (Zoom)', 'Online (Zoom)'),
}
```

### Крок 3: Використовуйте `getContent()` у компонентах

```typescript
import { useMultilingualContent } from '../utils/multilingualData';

const MyComponent = () => {
  const { getContent } = useMultilingualContent();
  
  return (
    <div>
      <h1>{getContent(event.title)}</h1>
      <p>{getContent(event.description)}</p>
    </div>
  );
};
```

---

## 🎯 Що потрібно перекласти далі:

### 1. **Решта подій** (`src/data/events.ts`)
- [ ] Події 7-20+ (всі інші)

### 2. **Новини** (`src/data/news.ts`)
- [ ] Всі новини

### 3. **Оголошення** (`src/data/announcements.ts`)
- [ ] Всі оголошення

### 4. **Документи** (`src/data/documents.ts`)
- [ ] Назви та описи документів

### 5. **Сторінки**
- [ ] `About.tsx` - всі секції
- [ ] `Activity.tsx`
- [ ] `Education.tsx`
- [ ] `Teachers.tsx`
- [ ] `Students.tsx`
- [ ] `Resources.tsx`
- [ ] `Contacts.tsx`

### 6. **Компоненти**
- [ ] `Footer.tsx`
- [ ] `Announcements.tsx`
- [ ] Всі інші компоненти з хардкодженим текстом

---

## 🚀 Автоматизація:

### Знайти всі хардкоджені українські рядки:

```bash
# Знайти всі файли з українським текстом
grep -r "title: '[А-Яа-яІіЇїЄєҐґ]" src/data/ src/pages/ src/components/
```

### Перевірити, що всі переклади використовуються:

```bash
# Знайти використання getContent
grep -r "getContent" src/
```

---

## 💡 Поради:

1. **Для коротких рядків** використовуйте `ml()` прямо в даних
2. **Для довгих текстів** краще використовувати файли перекладів (`locales/`)
3. **Для UI елементів** використовуйте `useTranslation('common')` або `useTranslation('navigation')`
4. **Для контенту сторінок** використовуйте `useTranslation('pages')` або специфічні namespace

---

## 🔄 Міграція існуючого контенту:

### Автоматичний пошук та заміна:

```bash
# Приклад для events.ts
# Знайти: title: 'Текст',
# Замінити на: title: ml('Текст', 'Translation'),
```

**Важливо:** Завжди перевіряйте результати автоматичної заміни!

---

## ✅ Перевірка:

1. Перемкніть мову на EN
2. Перевірте, що контент перекладається
3. Перевірте консоль на помилки
4. Запустіть `npm run validate:translations`

---

## 📚 Приклади перекладів:

### Події:
- Вебінар → Webinar
- Курс → Course
- Семінар → Seminar
- Олімпіада → Olympiad
- Конкурс → Competition

### Локації:
- Онлайн → Online
- Київ → Kyiv
- Академія → Academy
- корп. → building
- ауд. → room

### Організатори:
- Відділ → Department
- Кафедра → Department
- Оргкомітет → Organizing Committee
- Науково-методичний центр → Scientific and Methodological Center

---

**Наступний крок:** Перекласти решту подій та новин! 🚀

