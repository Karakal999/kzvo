# Академія Педагогічної Освіти

Сучасний веб-сайт для Академії педагогічної освіти, побудований з використанням актуальних технологій.

## Технологічний стек

- **React 18** - Бібліотека для побудови користувацького інтерфейсу
- **TypeScript** - Типізований JavaScript для підвищення надійності коду
- **Vite** - Швидкий збірник для сучасних веб-додатків
- **Tailwind CSS** - Утилітарний CSS-фреймворк для швидкої розробки UI
- **React Router DOM** - Маршрутизація для односторінкового додатку
- **Lucide React** - Сучасні SVG іконки
- **Firebase** - Backend для аутентифікації та бази даних
- **React Hook Form** - Управління формами
- **i18next** - Інтернаціоналізація (українська/англійська)

## Структура проєкту

```
src/
├── components/     # Повторно використовувані компоненти (Header, Footer)
├── layouts/       # Макети сторінок (MainLayout)
├── pages/         # Сторінки сайту (Home, About, Programs, тощо)
├── hooks/         # Кастомні React хуки
├── utils/         # Допоміжні функції
├── types/         # TypeScript типи та інтерфейси
├── data/          # Мокові дані для розробки
└── assets/        # Статичні файли (зображення, шрифти)
```

## Кольорова схема

Проєкт використовує фірмові кольори Академії:

- **Основний синій**: `#1e3a8a` (primary)
- **Акцентний жовтий**: `#fbbf24` (accent)

Кольори налаштовані в `tailwind.config.js` і доступні через класи Tailwind:
- `bg-primary`, `text-primary` - основний синій
- `bg-accent`, `text-accent` - акцентний жовтий

## Встановлення та запуск

### Встановлення залежностей

```bash
npm install
```

### Налаштування Firebase

Перед запуском додатку необхідно налаштувати Firebase для аутентифікації:

1. Створіть проект в [Firebase Console](https://console.firebase.google.com/)
2. Увімкніть Authentication (Email/Password)
3. Створіть Firestore Database
4. Скопіюйте конфігурацію Firebase
5. Створіть файл `.env` в корені проекту:

```env
VITE_FIREBASE_API_KEY=your_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
```

**Детальні інструкції:** Дивіться [FIREBASE_SETUP.md](./FIREBASE_SETUP.md)

### Запуск у режимі розробки

```bash
npm run dev
```

Додаток буде доступний за адресою `http://localhost:5173`

### Збірка для production

```bash
npm run build
```

### Попередній перегляд production-збірки

```bash
npm run preview
```

## Доступні сторінки

### Публічні сторінки
- **/:lang** - Головна сторінка (uk/en)
- **/:lang/about** - Про академію
- **/:lang/activity** - Діяльність
- **/:lang/education** - Освіта
- **/:lang/teachers** - Для вчителів
- **/:lang/students** - Для учнів
- **/:lang/resources** - Ресурси
- **/:lang/news** - Новини
- **/:lang/events** - Події
- **/:lang/competitions** - Конкурси
- **/:lang/programs** - Освітні програми
- **/:lang/contacts** - Контакти

### Аутентифікація
- **/login** - Вхід в систему
- **/register** - Реєстрація нового користувача
- **/profile** - Профіль користувача (потрібна авторизація)

### Адміністрування
- **/admin** - Адмін-панель (тільки для адміністраторів)
  - Управління користувачами
  - Управління контентом
  - Управління подіями
  - Налаштування системи

## Розробка

### Додавання нових компонентів

Створюйте нові компоненти в папці `src/components/`:

```typescript
// src/components/MyComponent.tsx
const MyComponent = () => {
  return <div>My Component</div>;
};

export default MyComponent;
```

### Додавання нових сторінок

1. Створіть компонент сторінки в `src/pages/`
2. Додайте роут в `src/App.tsx`

### Робота з типами

Визначайте TypeScript типи в `src/types/index.ts`:

```typescript
export interface MyType {
  id: string;
  name: string;
}
```

## 🔐 Система аутентифікації

Сайт має повноцінну систему реєстрації та входу користувачів з двома рівнями доступу:

### Функції для користувачів:
- ✅ Реєстрація з email та паролем
- ✅ Вхід в систему
- ✅ Вихід з системи
- ✅ Скидання пароля
- ✅ Перегляд та редагування профілю

### Функції для адміністраторів:
- ✅ Управління користувачами
- ✅ Призначення ролей (user/admin)
- ✅ Управління контентом (в розробці)
- ✅ Управління подіями (в розробці)

### Використання в коді:

```tsx
import { useAuth } from './context/AuthContext';

function MyComponent() {
  const { user, userProfile, signIn, signOut, isAdmin } = useAuth();
  
  if (!user) {
    return <div>Будь ласка, увійдіть</div>;
  }
  
  return (
    <div>
      <p>Привіт, {userProfile?.displayName}!</p>
      {isAdmin && <p>Ви адміністратор!</p>}
      <button onClick={signOut}>Вийти</button>
    </div>
  );
}
```

### Захищені роути:

```tsx
import { ProtectedRoute } from './components/auth/ProtectedRoute';

<Route path="/admin" element={
  <ProtectedRoute requireAdmin={true}>
    <AdminPanel />
  </ProtectedRoute>
} />
```

## 🧪 Testing

### Unit Tests
```bash
npm run test              # Run tests
npm run test:ui           # Run tests with UI
npm run test:coverage     # Run tests with coverage
```

### E2E Tests
```bash
npm run cypress:open      # Open Cypress UI
npm run cypress:run       # Run Cypress headless
npm run test:e2e          # Run full E2E suite
```

### Translation Validation
```bash
npm run i18n:validate     # Validate translations
npm run i18n:performance  # Check performance
```

**Test Coverage:**
- ✅ 16 unit tests (100% passing)
- ✅ 20+ E2E scenarios
- ✅ Translation validation
- ✅ Performance monitoring

See [TESTING_I18N.md](./TESTING_I18N.md) for detailed testing guide.

## Ліцензія

© 2024 Академія Педагогічної Освіти. Усі права захищені.
